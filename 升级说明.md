# 知行记 V2 升级说明

对应站点：https://zhangdade123.github.io/zhixingji/

新版仍是单个 `index.html`，沿用原界面和 `zhixingji-v1` 数据键。已核对 GitHub 仓库 HEAD a70582b 中的网页与提供的原文件一致（换行格式除外）。

## 使用和发布

1. 在旧站点的“我的 → 数据备份”导出一份 JSON；关闭其他正在编辑知行记的标签页。
2. 将发布包中的 `index.html` 上传覆盖 GitHub 仓库 `zhangdade123/zhixingji` 的同名文件。等待 GitHub Pages 部署完成，再在原网址强制刷新（Ctrl+F5）。保持原网址，以便使用原来的浏览器存储。
3. 到“我的 → 关于知行记”附近确认显示 **版本 V2**；再检查原来的待办、笔记是否完整。
4. 打开“网页内提醒”和“Windows / 系统通知”，进入“通知诊断与测试”，点击“授权并发送测试通知”。确认 Windows 横幅或通知中心实际出现，再新建一个未来两分钟提醒的事项测试。
5. 如需自动邮件，在 Supabase SQL Editor 部署新版 `知行记-后端定时邮件.sql`，再开启并同步“重要事项邮件提醒”。EmailJS 模板收件人需使用 `{{to_email}}`；服务端请求是否允许、额度与配额需在 EmailJS 账户中确认。

`cloud-schema.sql` 是同步表的安全建表/迁移脚本；已正常使用复合主键和账号 RLS 的项目不必为本次前端升级重复迁移。脚本发现无账号归属的历史记录会停止，不会自动删除这些记录。

**本次尚未执行线上部署和 Supabase SQL。** 当前环境没有 GitHub 推送凭据，也没有 Supabase 管理连接。代码完成不等于线上已经更新，SQL 静态核对不等于真实数据库执行验证。

## 已落实

- 启动不再把旧数据提升为“最新修改”，云同步使用版本条件写入；冲突暂停并提供双方备份与版本选择。
- 历史 JSON 先校验/迁移，损坏数据禁止覆盖；保存失败明确提示，支持紧急导出。
- 快速切换笔记、置顶或重绘时先保存草稿，取消旧计时器；富文本执行白名单清洗。
- 普通附件保存在 IndexedDB 中，保存真实文件内容，并提供下载与移除。完整备份含本机文件；恢复前下载当前备份并为恢复文件分配新 ID。
- API Key 迁移到独立本机存储，不进入新备份和后续云同步。此前已经导出或同步过的历史副本不会自动消失。
- 网页内提醒与系统通知独立开关；系统失败可重试；延后提醒会再次使用系统通道；未确认提醒持久化，刷新后恢复。
- 新增通知诊断，显示权限、协议、上次检查、未确认数和错误；回到前台立即补查。
- 重复事项完成/撤销/再完成保持幂等；月末按目标月最后一天，并保留系列锚点；提前提醒的日期偏移保持一致。
- 隐藏尚未实现的自定义重复选项；修正回顾重复计数、无时间事项编辑副作用、中文输入法 Enter 误提交、跨午夜日期与历史总结范围。
- 事项和提醒按北京时间（UTC+8）解释，和邮件后端一致；AI 草稿固定生成时的日期范围，说明材料截断及附件仅发送名称。
- 提供弹窗焦点管理、键盘焦点循环、开关可访问状态和 CSV 公式字符保护。

## 仍有明确边界

- **关闭网页后的 Windows 定时推送尚未实现。** 当前每 15 秒检查一次；后台节流、系统休眠、Windows 勿扰仍会影响及时展示。需要该能力时，应另行部署服务端 Web Push/Service Worker 和订阅管理，单纯安装 PWA 不会自动获得定时唤醒。
- **附件本体目前仅在此浏览器/此设备。** 云同步只同步附件清单；换设备用完整备份迁移，或重新添加原文件。旧版只有文件名的记录无法凭空恢复原文件。
- **云同步是冲突保护和版本选择，不是逐条自动合并。** 双方都修改时不会静默覆盖；保留某一版本前下载备份。多标签检测到外部修改时暂停自动保存，请先导出当前草稿，再关闭多余标签并刷新。
- **自动邮件改为后端独占。** 未部署新版 SQL 时，网页不会自动发信。“发送测试邮件”仍是用户点击后的一次前端测试。后端每分钟最多处理一封；大量积压会排队。
- 邮件 `accepted` 只表示 EmailJS 接受请求。429 会有限退避重试；超时/500 等不确定结果记为 `unknown`，避免盲目重发造成重复。需先核验 EmailJS 记录再决定人工重试。
- 网页没有迁移既有云库里所有历史凭据副本；新成功同步后当前整库数据将不含 AI Key。高敏感使用场景仍建议采用后端 AI 代理。
- 没有进行真实邮件发送、数据库部署、真实账号登录及 Windows 横幅展示测试。浏览器回归使用隔离数据和模拟服务。

## 邮件验收

部署 SQL 后，先看 `cron.job_run_details` 中是否成功执行，再看 `zxj_private.mail_outbox` 的状态。仅在 cron.job 中看见任务不能证明发信成功。用户关闭邮件开关、完成/删除/改期会在发送前重新检查；已经入网请求不能撤回。

pg_net 队列含收件信息，脚本撤销了匿名与普通登录角色对队列表的直接访问；如果 Supabase 项目另有应用直接访问这些内部表，需由管理员核验其权限依赖。

## 回退

发布包包含 `index.before-v2.html`。需要回退时将其改名 `index.html` 后上传覆盖。V2 新增真实附件保存在 IndexedDB，旧版没有相应下载能力；回退前先导出 V2 完整备份。发生数据损坏请保留原始备份，不要清理浏览器站点数据。

## 验证材料

`upgrade-test-results.json` 记录自动检查结果；`verify-upgrade.cjs` 是可重复运行的测试脚本。SQL 仅做静态核对和官方 API 语义核验，未连接真实 Postgres 执行。

官方依据：[EmailJS send API](https://www.emailjs.com/docs/rest-api/send/)、[Supabase pg_net](https://supabase.com/docs/guides/database/extensions/pg_net)。
