# 云端为准的记录保存（2026-09-09）

登录或重新打开网页后，先读取当前账号最新云端记录。旧缓存、历史 dirty 标记及上一账号的数据不会自动上传；空账号建立空白记录。

事项、笔记和账号设置的修改需要联网。编辑中的内容只暂存在内存；收到云端确认后才更新缓存并显示保存成功。关闭前请等待“正在保存到云端”提示消失。离线可查看缓存，未提交修改不会在联网后补写。

保存使用现有 zxj_sync.rev 条件更新。其他设备先更新时，本次旧版本修改不提交，自动采用最新云端记录并提示重新操作。当前以整份账号记录为版本单位，因此同时修改不同事项也可能需要重新操作；不会自动合并或覆盖云端。重复事项的完成与下一次安排在同一次条件更新中提交。

账号切换、退出登录及请求失败后不补写未确认修改。未提交草稿仅尝试保存在本机恢复快照，可手动导出，不参与同步。浏览器权限、设备推送订阅和 API Key 仍按设备管理。

无需新增数据库脚本。邮件定时任务和后台推送沿用已部署服务；本次未向真实事项发送测试通知。

本地验证：verify-cloud-authority.cjs 覆盖旧缓存登录、待确认修改不落盘、过期页面覆盖阻止、双设备并发完成、离线与重连、连续修改、请求失败、实际笔记编辑及冲突、手机布局、退出与在途请求、空账号。verify-app-startup.cjs 使用云端保存设置验收默认页。提醒与推送工作器回归另行运行。
