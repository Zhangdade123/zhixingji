# 知行记 V3

## 附件跨设备同步

1. 在 Supabase 项目的 SQL 编辑器执行 `知行记-附件存储.sql`。本次网页发布不等于已经执行数据库脚本。
2. 原设备登录原账号，完成数据同步。在「我的 → 附件跨设备同步」上传旧附件或重试失败项。
3. 新添加的附件先写入本机，保存到待办或笔记后，每 30 秒检查并上传。页面需要保持打开。失败会保留原文件，可手动重试。
4. 显示「仅本机／上传中／已同步」；已同步表示原文件上传成功，附件清单仍需完成账号数据同步。失败原因可通过重试时提示查看。
5. 其他电脑或手机登录同一账号、同步数据后，点击文件名下载原文件，并尽力缓存到本机。

私有 bucket 为 `zxj-files`，路径为用户 UID/随机文件 ID。只允许当前用户读取和新增自己的文件，不开放公开链接、覆盖和删除。单个文件不超过 20MB。移除引用或清空回收站不会自动删除云对象，后续可在 Supabase 存储控制台管理空间。完整备份仍只打包本机已有文件，云端文件可先点击下载以缓存。

SQL 使用 Supabase 官方 Storage RLS 模式：https://supabase.com/docs/guides/storage/security/access-control

## 每周回顾与下周安排

入口：「我的 → 每周回顾与下周安排」。一周为北京时间周一至周日，可选择历史周。

- 完成事项按完成时间，新增笔记按创建时间归档。
- 延期事项包含截至所选周末且目前仍逾期的事项，以及本周记录的向后改期事项。此前版本没有保存的改期历史无法还原。
- 勾选后选择下周某一天。未完成事项直接改期并清除旧提醒；已完成事项或笔记生成关联来源的后续待办，新待办不设置自动提醒。
- 同一来源同一目标周不会重复创建，回收站也参与防重复。未完成事项保留原检查清单和附件。
- 这是打开时自动整理，不是后台定时生成或邮件推送。

## 子任务与检查清单

新建或编辑待办时，可添加、删除、改名、勾选步骤，或使用论文步骤模板。保存后列表显示完成数和进度条。勾选全部步骤不会自动完成父事项，避免误触重复事项生成。下一次重复事项会继承步骤名称并重置勾选状态。

## 验证范围

原有 30 项回归检查通过。新增检查覆盖创建与编辑清单、重复事项重置、周界限与防重复、上传失败后重试、重复附件引用、授权下载原字节、上传中切换账号、非法云路径和手机布局。

文件上传与下载测试使用隔离浏览器和模拟 Storage 响应；真实 Supabase 存储尚未初始化与验收。网页不保存服务端管理员密钥。
