# 后台事项推送

## 行为

- 电脑网页：到点前已打开、联网且正常运行的设备各自提醒。后来才打开或恢复联网的页面不补弹。网页后台计时可能被系统节流，超过两分钟的执行间隔按恢复处理。
- 手机：开启后台事项提醒后，通过服务器推送，不依赖页面是否打开。注册时已过期的事项不补发。
- 推送适用于所有未完成事项的提醒时间与截止时间，与只发高优先级的邮件独立。
- 当前扫描每分钟执行一次；每轮最多发送五条设备通知。推送 TTL 为 60 秒，离线超过有效期不保证补送。系统勿扰、网络和推送服务仍会影响实际横幅。
- 单设备开启后台推送后，其网页不会再次发送相同的本地系统通知；网页内卡片仍可显示。延后十分钟仅作用于当前网页设备。

## 部署

1. 在项目 SQL Editor 执行完整 `push-reminders.sql`。这是新增推送表与调度任务，不改邮件配置。只读检查：

```sql
select jobname,active,schedule from cron.job where jobname='zxj-push-reminders';
```

2. 进入 Supabase → Edge Functions → 新建函数，名称必须为 `zxj-push-reminders`。把 `zxj-push-reminders.ts` 全文作为入口代码粘贴并部署。
3. 在该函数设置中关闭网关的 Verify JWT。函数内部会验证用户访问令牌；定时调度使用数据库生成的独立 worker secret。不要把 service role key 或推送私钥放进网页。
4. 手机上用 Safari 添加本站到主屏幕，从桌面图标打开，登录同一账号。在“我的 → 后台事项提醒”点击“开启此设备后台提醒”，允许系统通知，确认订阅保存成功。每台需要后台提醒的设备分别开启。
5. 电脑新增未来三分钟的未完成事项，设置提醒时间或截止时间，确认云同步成功。关闭手机应用，保持手机联网，观察系统通知；再验证手机前台场景。

## 一行诊断（不输出订阅端点或密钥）

```sql
select j.active as cron_active,
 (select count(*) from public.zxj_push_reminders_subscriptions) as subscribed_devices,
 r.status as last_run,r.return_message,
 (select jsonb_agg(x) from (select state,error,due_at,sent_at from public.zxj_push_reminders_jobs order by created_at desc limit 5) x) as recent_pushes
from cron.job j left join lateral (
 select status,return_message from cron.job_run_details where jobid=j.jobid order by start_time desc limit 1
) r on true where j.jobname='zxj-push-reminders';
```

`accepted` 仅表示推送服务接受请求，手机实际横幅还需本人确认。401 检查 Verify JWT 和函数内认证，404 检查函数名称与部署，410 需在设备上重新开启订阅。

## 验证范围

本地 PostgreSQL 引擎执行完整 SQL（本地替代 pg_cron/pg_net 外部功能），验证设备分发、后注册设备不补发、唯一键去重、原子领取、私有表权限。Edge Function 隔离测试验证鉴权、端点限制、发送内容、已完成与过期事项跳过。网页回归及 iOS/Android/普通浏览器启动测试通过。真实推送部署、密钥初始化、手机授权与横幅仍待验收。

参考：
- https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/
- https://supabase.com/docs/guides/functions/quickstart-dashboard
