# 提醒修复（2026-09-07）

截图显示邮件时间 18:50，正文截止时间 09:50。代码确认网页模式每 15 秒扫描一次，并会在页面恢复后补发启用以来的逾期事项；截图本身无法确认邮件服务实际接受时间。北京时间解析已通过测试。

## 本次改动

- 同账号跨设备在显示提醒前，使用已有 zxj_sync 表单独记录每次事项、提醒类型、时间的领取状态。唯一键原子插入；另一设备不重复显示。数据行使用 receipt: 前缀，不修改 main 正文版本，不需要新增数据库表。
- 断网、登录未完成、账号冲突时等待核验云端，不用本地状态猜测其他设备是否已提醒。未登录且未启用云同步时仍支持本机提醒。
- 延后 10 分钟先同步云端；到期后采用版本条件更新，只允许一台设备领取。不同截止时间会产生新提醒。
- 已有本机提醒记录在设备升级并重新检查后写入云端。旧版本设备必须刷新到新版；从未上传的历史本机记录无法由另一设备凭空恢复。
- 网页邮件迟到超过 5 分钟不再补发，状态显示“已错过发送时间”。这是补发保护，不能替代后台调度。
- 网页邮件也使用独立云端记录去重；后端模式随账号设置同步，防止另一设备继续使用旧网页模式。

## 关闭网页后仍按时发送：尚需真实后端部署

1. 在 Supabase 项目的 SQL Editor 执行同目录 `知行记-后端定时邮件.sql`。该脚本已有独立队列、去重、北京时间转换和每分钟定时任务。
2. 在网页“我的 → 邮件发送设置”选择“后端定时发送”，填写发送通道和收件邮箱，打开“重要事项邮件提醒”；等待云同步成功。
3. 新建未来 3 分钟到期的高优先级事项，确认已同步，再关闭所有网页。以这个新测试事项验证，不对真实逾期事项盲目重发。
4. 在 SQL Editor 检查：

```sql
select jobname, active from cron.job where jobname='zxj_mail_v2';
select status, return_message, start_time
from cron.job_run_details order by start_time desc limit 10;
select state, due_at, requested_at, updated_at, error
from zxj_private.mail_outbox order by updated_at desc limit 10;
```

后端当前每分钟最多发送一封；积压会导致额外等待。EmailJS 接受请求与收件箱送达分别验收。服务端 EmailJS 请求能否通过也需真实验证，不能仅凭前端测试成功判断。

本次未访问真实账号、未执行数据库脚本、未发送测试邮件，未推送上线。部署后应比较到期、请求、收件三个时间。不要把取消迟到补发视为已解决后台准时发送。

## 本地验证

运行 `node build-upgrade.cjs`、`node verify-upgrade.cjs` 和 `node verify-reminder-cloud.cjs`。浏览器回归覆盖页面功能、北京时间、及时邮件、9 小时迟到不补发、模式同步；隔离模拟覆盖双设备并发、重新打开、账号隔离、改期、延后竞争、离线与发送去重。
