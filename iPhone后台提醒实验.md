# iPhone 后台提醒：最小实验

本次只验证一条两分钟延迟测试通知，不启用真实待办后台调度。

## 在 Supabase 部署（仅首次）

1. 在当前项目 SQL Editor 新建查询，复制 `push-pilot.sql` 全文并运行。
2. 打开左侧 Edge Functions，创建函数，名称必须为 `zxj-push-pilot`。通过编辑器部署，将 `zxj-push-pilot.ts` 全文粘贴为入口代码。
3. 在函数设置关闭网关的 **Verify JWT / Enforce JWT Verification**。函数内部仍用 `getUser` 校验用户，定时任务使用数据库生成的私有密钥校验；这不表示允许匿名预约或发送。
4. 发布函数。不用手动填写 VAPID 密钥、不用把管理员密钥放进网页。代码使用 Supabase Edge Functions 内置的服务端环境变量，首次授权请求会生成并私有保存 VAPID 密钥。

若函数编辑器无法解析 npm 导入或部署报错，请提供错误文字；不要发送密钥。以上部署当前尚未代为执行。

## 在 iPhone 13 Pro / iOS 17.6.1 测试

1. Safari 打开 https://zhangdade123.github.io/zhixingji/ ，分享 → 添加到主屏幕。
2. 从桌面「知行记」图标进入，在「我的 → 账号与同步」登录同一账号。主屏幕应用的本机数据可能与 Safari 分开，先登录同步，不要导入覆盖。
3. 「我的 → iPhone 后台提醒实验」→ 授权并预约两分钟测试。
4. **只有看到预约成功后**，返回主屏幕并锁屏。保持联网、允许通知，测试时关闭专注模式。
5. 预计预约后 2～3 分钟触发。记录是否看到了横幅、声音或锁屏通知。
6. 若没有看到，重新进入实验页点击「检查最近测试结果」。pending 表示服务端尚未领取；accepted 仅表示推送服务接受，不保证手机实际显示；failed / unknown 表示失败或发送结果不确定。

## 实验设计和边界

- SQL 每分钟检查队列，仅有到期任务才调用 Edge Function；不用前台 setTimeout 延迟发送。
- 单个账号最多一个正在处理的测试。重复点击不会并发重复发送；测试记录七天后自动清理。
- 未允许通知不会预约任务。服务端只允许苹果、Google、Mozilla、Windows 的受支持推送域名，拒绝任意 URL。
- 通知点击回到「我的」。测试通知没有个人待办内容。
- 不重试结果不确定的发送，避免产生重复提示。
- 本次没有添加离线业务缓存，也不保证断网提醒；验证通过再继续手机界面及真实待办调度。

停止实验调度：在 SQL Editor 执行 `select cron.unschedule('zxj-push-pilot');`。不要删除 VAPID 配置，否则已有订阅需要重新建立。
